# Environment template for the Elon container only.
# TEMPLATE ONLY: copy this file to `elon.env` and fill in real values locally.
# Create the real file with `cp elon.env.example elon.env` before running the container.

# === Provider Fallback ===
# From the Z.ai dashboard > GLM Coding Plan > API Keys. Tier-3 fallback only.
ZAI_CODING_PLAN_KEY="<paste-zai-coding-plan-key-here>"

# === Bridge Access ===
# Generated during bridge setup so Elon can call Milo's MCP surface.
MILO_MCP_TOKEN="<generate-milo-mcp-token>"

# === Publishing Attribution ===
# Dedicated Twitter/X API bearer token used only when Elon dispatches Zuck for publishing.
ZUCK_TWITTER_TOKEN="<paste-zuck-twitter-token-here>"
# Dedicated LinkedIn API token used only when Elon dispatches Zuck for publishing.
ZUCK_LINKEDIN_TOKEN="<paste-zuck-linkedin-token-here>"

# === Observability (Optional) ===
# Optional NDJSON ingest endpoint for centralized log forwarding; leave blank for local-only logging.
SIEM_ENDPOINT=""
